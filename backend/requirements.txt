# FastAPI core dependencies (lightweight, install first)
# Models retrained with sklearn 1.7.2
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic>=2.8.0
pydantic-settings>=2.3.0
python-dotenv==1.0.0

# Database and storage (install before heavy ML packages)
supabase==2.10.0
boto3==1.29.7

# Core scientific computing (numpy first, others depend on it)
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.7.2
joblib==1.4.2

# ML models (heavy packages, install after dependencies)
xgboost==2.1.3
shap==0.46.0

# Visualization (for EDA scripts, not runtime critical)
matplotlib==3.8.2
seaborn==0.13.0

# Dataset utilities
ucimlrepo==0.0.3

# OpenAI for natural language explanations
openai==1.54.0
